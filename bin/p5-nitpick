#!/usr/bin/env perl
use strict;
use warnings;

use App::P5Nitpick;
use App::P5Nitpick::Nitpicker;

my @rules = qw( RemoveUnusedImport QuoteSimpleStringWithSingleQuote );

for my $file (@ARGV) {
    my $x = App::P5Nitpick::Nitpicker->new( file => $file, rules => \@rules );
    $x->rewrite;
}
